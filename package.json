{
  "name": "vm-tipping-2026",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "web",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently -n shared,server,web -c blue,green,magenta \"npm run dev -w @vm-tipping-2026/shared\" \"npm run dev -w server\" \"npm run dev -w web\"",
    "build": "npm run build -w @vm-tipping-2026/shared && npm run build -w server && npm run build -w web",
    "start": "npm run start -w server",
    "pretest": "npm run build -w @vm-tipping-2026/shared",
    "test": "vitest run && npm run test -w server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.15"
  },
  "dependencies": {
    "@hono/node-server": "^2.1.1",
    "hono": "^4.13.8"
  },
  "overrides": {
    "brace-expansion@1": "^1.1.21",
    "brace-expansion@2": "^2.1.7",
    "uuid": "^11.1.1"
  }
}
